// var editor = gm.Editor;
// var aiRenderer = editor.renderer.ai = {};
// var navGridRenderer;

// aiRenderer.init = function(level) {
// 	navGridRenderer = new gm.Renderer.NavMap(aiWorld._navGrid._navMap);
// };

// aiRenderer.render = function(ctx, bbox) {
// 	var navGrid = level._aiWorld._navGrid;
// 	var tilesize = navGrid._navMap.tilesize;
// 	navGridRenderer.render(ctx, navGrid._tx * tilesize, navGrid._ty * tilesize, bbox);
// };